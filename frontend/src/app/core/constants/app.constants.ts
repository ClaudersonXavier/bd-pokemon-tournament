export const TRAINER_AVATAR_KEY  = (userId: number | string) => `trainer-avatar-${userId}`;
export const REDIRECT_DELAY_MS   = 1200;
export const PASSWORD_MIN_LENGTH = 6;
export const NAME_MIN_LENGTH     = 3;
